export const activeNavTitle = (state) => {
  return state.activeNav.title;
};

export const activeNavCurrent = (state) => {
  return state.activeNav.current;
};

export const bottomNavIsShow = (state) => {
  return state.bottomNavIsShow;
};